services:
  - type: web
    name: my-mcp-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m mcp run server.py --transport streamable-http --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
